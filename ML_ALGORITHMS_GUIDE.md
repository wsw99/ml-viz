# 机器学习算法总结：应用场景 · 评估指标 · 面试考点

---

## 1. K-Means 聚类

### 应用场景
- 用户分群（电商、广告投放）
- 图像颜色压缩（将像素颜色量化为 K 种）
- 文档主题聚类
- 异常检测（距所有质心都远的点视为异常）
- 基因表达数据分组

### 评估指标

| 指标 | 全名 | 说明 |
|------|------|------|
| WCSS | Within-Cluster Sum of Squares / 簇内平方和 | 越小越好；Elbow Method 用它选 K |
| Silhouette Score | 轮廓系数 | 范围 [−1, 1]，越大越好；s=(b−a)/max(a,b) |
| DBI | Davies-Bouldin Index / 戴维斯-鲍尔丁指数 | 越小越好 |
| CHI | Calinski-Harabasz Index / 卡林斯基-哈拉巴斯指数 | 越大越好 |

### 面试考点

**Q: K-Means 的收敛性能保证吗？**
能收敛到局部最优，但不保证全局最优。常用多次随机初始化取最优（sklearn 默认 `n_init=10`）。

**Q: 如何选择 K？**
Elbow Method（看 WCSS 拐点）+ Silhouette Score 验证。

**Q: K-Means 的缺点？**
① 需要预设 K；② 对初始质心敏感；③ 假设簇为球形且大小相近；④ 对异常值敏感；⑤ 不适合非凸形状的簇（用 DBSCAN 更好）。

**Q: K-Means++ 做了什么改进？**
初始化时让质心尽量分散：每个新质心以距已选质心距离的平方为概率采样，降低陷入差局部最优的概率。

**Q: 时间复杂度？**
O(n · K · d · I)，n=样本数，K=簇数，d=维度，I=迭代次数。

---

## 2. 线性回归 (Linear Regression)

### 应用场景
- 房价预测、销量预测
- 股票价格趋势（短期）
- 医疗中预测连续指标（血压、BMI）
- 工程中建立变量间线性关系

### 评估指标

| 指标 | 全名 | 说明 |
|------|------|------|
| MSE | Mean Squared Error / 均方误差 | (1/n)Σ(ŷ−y)²，对大误差惩罚更重 |
| RMSE | Root MSE / 均方根误差 | √MSE，与目标变量同单位，便于解释 |
| MAE | Mean Absolute Error / 平均绝对误差 | (1/n)Σ|ŷ−y|，对异常值更鲁棒 |
| R² | Coefficient of Determination / 决定系数 | 1−SS_res/SS_tot，范围 [0,1]，越大越好 |

### 面试考点

**Q: 线性回归的假设是什么？**
① 线性关系；② 残差独立；③ 残差同方差（homoscedasticity）；④ 残差正态分布；⑤ 无完全多重共线性。

**Q: 梯度下降 vs 正规方程？**
正规方程：w = (XᵀX)⁻¹Xᵀy，解析解，小数据集快。特征多时 O(d³) 求逆代价大，改用梯度下降。

**Q: L1 和 L2 正则化的区别？**
- L2（Ridge）：惩罚项 λΣwᵢ²，权重趋近于零但不等于零，适合特征都有用时。
- L1（Lasso）：惩罚项 λΣ|wᵢ|，权重可被压缩到精确为零，自动做特征选择。
- ElasticNet：两者结合。

**Q: 多重共线性如何处理？**
VIF 检测；删除相关性高的特征；PCA 降维；使用 Ridge 正则化。

**Q: 为什么用 MSE 而不是 MAE 作为损失函数？**
MSE 处处可微（MAE 在 0 处不可微），梯度计算更方便；但 MSE 对异常值更敏感。

---

## 3. 逻辑回归 (Logistic Regression)

### 应用场景
- 二分类：垃圾邮件检测、疾病诊断（患病/健康）、信用违约预测
- 多分类（OvR 策略）：手写数字识别
- 作为神经网络输出层激活函数
- 需要输出概率而非纯类别标签时

### 评估指标

| 指标 | 全名 | 说明 |
|------|------|------|
| Accuracy | 准确率 | (TP+TN)/总数；类别不平衡时失效 |
| Precision | 精确率 | TP/(TP+FP)；关注"预测为正"的质量 |
| Recall | 召回率 | TP/(TP+FN)；关注"实际为正"的覆盖 |
| F1 | F1 Score | 2×P×R/(P+R)；不平衡数据首选 |
| AUC-ROC | ROC 曲线下面积 | 衡量模型整体排序能力，[0,1]，0.5=随机 |
| Log Loss | 二元交叉熵 | −(1/n)Σ[y·log(ŷ)+(1−y)·log(1−ŷ)] |

### 面试考点

**Q: 逻辑回归为什么用交叉熵而不是 MSE？**
Sigmoid + MSE 会导致损失函数非凸（有多个局部最优），梯度消失严重。交叉熵 + Sigmoid 是凸函数，有唯一全局最优，梯度形式简洁：∂L/∂w = (ŷ−y)·x。

**Q: Sigmoid 函数的问题？**
梯度消失：输入绝对值大时梯度趋近于 0，在深层网络中导致训练缓慢（神经网络中用 ReLU 替代）。

**Q: Precision 和 Recall 如何权衡？**
取决于业务：医疗诊断宁可误报不可漏诊（优先 Recall）；垃圾邮件过滤宁可漏过不可错标正常邮件（优先 Precision）。通过调整分类阈值（默认 0.5）来控制。

**Q: 逻辑回归和线性 SVM 的区别？**
逻辑回归最大化对数似然（概率模型），SVM 最大化间隔（几何模型）；逻辑回归输出概率，SVM 输出类别；SVM 对异常值更鲁棒（只有支持向量起作用）。

**Q: 类别不平衡怎么处理？**
① 过采样（SMOTE）；② 欠采样；③ `class_weight='balanced'`；④ 用 F1/AUC-ROC 评估而非 Accuracy。

---

## 4. 决策树 (Decision Tree)

### 应用场景
- 医疗诊断（可解释性强）
- 信用评分、贷款审批
- 规则发现（特征选择）
- 作为集成模型（随机森林、XGBoost）的基学习器

### 评估指标

分类树使用 Accuracy / Precision / Recall / F1；回归树使用 MSE / MAE / R²。

树结构指标：

| 指标 | 说明 |
|------|------|
| Gini 不纯度 | 1−Σpᵢ²；越小越纯，范围 [0, 0.5] |
| 信息熵 Entropy | −Σpᵢlog₂pᵢ；越小越纯，范围 [0, 1] |
| 信息增益 IG | 父节点熵 − 分裂后子节点加权熵；越大越好 |
| 树深度 Depth | 控制模型复杂度的主要超参数 |

### 面试考点

**Q: Gini 和 Entropy 哪个更好？**
效果上几乎相同。Gini 计算更快（无对数运算）；sklearn 默认 Gini。

**Q: 决策树如何防止过拟合？**
① 限制 `max_depth`；② 限制 `min_samples_split` / `min_samples_leaf`；③ 剪枝（预剪枝 / 后剪枝 cost-complexity pruning）。

**Q: 决策树对特征缩放敏感吗？**
不敏感。分裂基于阈值比较，与特征量纲无关（这是相对于 SVM / KNN / 线性模型的优势）。

**Q: 决策树的缺点？**
① 容易过拟合；② 对数据微小变化敏感（高方差）；③ 贪心分裂不保证全局最优；④ 不适合线性关系数据。

**Q: ID3 / C4.5 / CART 的区别？**
- ID3：信息增益，只支持分类，倾向于选择取值多的特征。
- C4.5：信息增益率（修正 ID3 偏差），支持连续特征，可处理缺失值。
- CART：Gini 不纯度，只生成二叉树，同时支持分类和回归，sklearn 用此。

---

## 5. 随机森林 (Random Forest)

### 应用场景
- 高维特征的分类/回归（基因组数据、金融风控）
- 需要特征重要性排名
- 鲁棒性要求高、不希望调参太多的场景
- 医疗影像分类（传统方法时代）

### 评估指标

同决策树（分类：Accuracy/F1/AUC-ROC；回归：MSE/R²），额外有：

| 指标 | 说明 |
|------|------|
| OOB Error | 袋外误差；每棵树用未被 Bootstrap 采到的样本验证，免费得到验证集估计 |
| Feature Importance | 特征重要性；各特征在所有树中平均不纯度下降量，越大越重要 |

### 面试考点

**Q: 随机森林如何降低方差？**
两层随机性：① Bootstrap 采样（行随机）让每棵树训练数据不同；② 每次分裂只随机抽取 √d 个特征（列随机）让树之间相关性更低。多棵不相关的树取平均，方差显著降低（偏差基本不变）。

**Q: OOB 误差如何计算？**
每个样本只被平均 63.2% 的树用于训练（Bootstrap 采样约 63.2% 唯一样本），剩余树对该样本做预测，聚合这些预测得到 OOB 误差。可替代交叉验证。

**Q: 随机森林 vs XGBoost 如何选？**
- 随机森林：并行训练（树之间独立），调参少，不易过拟合，大数据友好。
- XGBoost：串行提升（每棵树纠正前一棵的残差），精度通常更高，但调参复杂，更容易过拟合。
- 工程上：先用随机森林快速建立 baseline，再用 XGBoost 调优。

**Q: 特征重要性的局限性？**
对高基数特征（取值多的类别特征）有偏；特征相关时重要性会被稀释分散；SHAP 值更准确。

**Q: 随机森林能处理缺失值吗？**
标准实现不直接处理（需先填充）；但 sklearn 的 `HistGradientBoostingClassifier` 和原始 Random Forest（Breiman 版）可以处理缺失值。

---

## 对比总结

| 维度 | K-Means | 线性回归 | 逻辑回归 | 决策树 | 随机森林 |
|------|---------|---------|---------|--------|---------|
| 任务类型 | 无监督聚类 | 有监督回归 | 有监督分类 | 分类/回归 | 分类/回归 |
| 可解释性 | 中 | 高 | 高 | 高 | 低 |
| 特征缩放 | 需要 | 建议 | 建议 | 不需要 | 不需要 |
| 过拟合风险 | 低 | 低 | 低 | 高 | 低 |
| 处理非线性 | 弱 | 不能 | 不能 | 能 | 能 |
| 训练速度 | 快 | 快 | 快 | 快 | 中 |
| 推理速度 | 快 | 快 | 快 | 快 | 中 |

---

*整理日期：2026-05-26*
